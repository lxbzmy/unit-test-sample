{
  "name": "02-mocha",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --recursive",
    "test:cov": "nyc --temp-dir=target/.nyc_output --reporter=html --reporter=text --reporter=lcov --report-dir=target/coverage mocha --recursive"
  },
  "devDependencies": {
    "chai": "^4.0.0",
    "mocha": "^10.0.0",
    "nyc": "^17.1.0"
  }
}
